// This hook is no longer needed as we're handling API calls directly in components
// Keeping this file for potential future use or to avoid import errors

export const useSummarizer = () => {
  // Placeholder implementation
  return {
    mutate: () => { },
    isLoading: false,
    data: null
  }
}
